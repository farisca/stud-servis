
<h2 align="center"> {{ 'CREATE_AD' | translate }}</h2>
<div class="container" style="position:relative; top:50px; width:400px;">
        
    <form class="form-register" ng-controller="JobsController as job" ng-submit="job.createNewJob()">
        <p>{{ 'AD_NAME' | translate }}</p>
        <label for="inputName" class="sr-only">{{ 'AD_NAME' | translate }}</label>
        <input type="text" ng-model="job.data.name" id="inputName" class="form-control" placeholder="" required autofocus><br>
         
        <div ng-controller="CategoryController">
        <p>{{ 'AD_CATEGORY' | translate }}</p>
        <label for="inputCategory" class="sr-only">Kategorija</label>

        <select class="form-control" id="select" ng-model="job.data.category_name"   ng-init="job.data.category_name='IT'">

        <option ng-repeat="category in podaci" ng-selected="{{category.id==1}}" value="{{category.name}}" >
        {{category.name}}</option>
        </select>
        <br/><br/>
        </div>
        
        <companies></companies>
        
        <p>{{ 'AD_DESCRIPTION' | translate }}</p>
        <label for="inputDescription" class="sr-only">{{ 'AD_DESCRIPTION' | translate }}</label>
        <input type="text" ng-model="job.data.description" id="inputDescription" class="form-control" placeholder="" required autofocus><br>
         
        <div ng-controller="LocationController">
        <p>{{ 'AD_LOCATION' | translate }}</p>
        <label for="inputLocation" class="sr-only">{{ 'AD_LOCATION' | translate }}</label>
        <select class="form-control" id="select" ng-model="job.data.city"  ng-init="job.data.city='Sarajevo'">
              <option ng-repeat="location in podaci" ng-selected="{{location.id==1}}" value="{{location.city}}">
                {{location.city}}
              </option>
            </select>
        <br/><br/>
        </div>
        <p>{{ 'AD_DURATION' | translate }}</p>
        <label for="inputDuration" class="sr-only">{{ 'AD_DURATION' | translate }}</label>
        <input type="datetime-local" ng-model="job.data.duration" id="inputDuration" class="form-control" placeholder="" required autofocus><br><br>

        <button class="btn btn-lg btn-primary btn-block" type="submit"><%= I18n.t('ad_button') %></button>
        <br>
        <div ng-show="job.isSuccess()" class="alert alert-success" role="alert">
          <span class="glyphicon glyphicon-exclamation-sign" aria-hidden="true"></span>
          {{job.successMsg}}
        </div>
        <div ng-show="job.isError()" class="alert alert-danger" role="alert">
          <span class="glyphicon glyphicon-exclamation-sign" aria-hidden="true"></span>
          <span class="sr-only">Greška:</span>
              {{job.errorMsg}}
        </div>

        

    </form>
    <br><br><br><br>
</div>